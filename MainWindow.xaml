<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NotShitAltTab.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NotShitAltTab"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NotShitAltTab">

	<Grid Background="#CC0F1113" Padding="16" KeyDown="Grid_KeyDown">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Better Alt+Tab — type first letter, W/S cycle duplicates, Enter to switch, Esc to hide"
                   Margin="0,0,0,12" Opacity="0.8"/>

		<ListView x:Name="WindowsList"
                  Grid.Row="1"
                  IsItemClickEnabled="True"
                  SelectionMode="Single"
                  ItemClick="WindowsList_ItemClick"
                  DoubleTapped="WindowsList_DoubleTapped">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="local:WinEntry">
					<StackPanel Orientation="Horizontal" Spacing="12">
						<Image Source="{x:Bind Icon}" Width="24" Height="24"/>
						<TextBlock Text="{x:Bind ProcessName}" Margin="8,0,0,0" Opacity="0.7"/>
						<TextBlock Text="{x:Bind Title}" FontSize="16"/>
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<TextBlock Grid.Row="2" x:Name="Hint"
                   Text=""
                   Margin="0,12,0,0" Opacity="0.7"/>
	</Grid>
</Window>